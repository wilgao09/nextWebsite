{
  "private": true,
  "scripts": {
    "dev": "cd scripts && node compileBlogs.js && cd .. && next dev",
    "build": "cd scripts && node compileBlogs.js && cd .. && next build",
    "start": "cd scripts && node compileBlogs.js && cd .. && next start"
  },
  "dependencies": {
    "markdown-to-jsx": "^7.1.7",
    "next": "latest",
    "raw-loader": "^4.0.2",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
